# Frontend

# API URL — leave EMPTY for production so the backend serves on the same origin.
# For local Vite dev, the proxy in vite.config.ts handles /api automatically.
VITE_API_URL=

# App title
VITE_APP_TITLE=PHQ Complaint Dashboard
